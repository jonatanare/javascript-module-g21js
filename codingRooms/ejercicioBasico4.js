/**
 * TODO: Generar una funcion que retorne la palabra(parametro) al reves reverseString('hola') // returns 'aloh'
 */
let reverseString = function(palabra) {
    for (let i = 0; i < palabra.length; i++) {
        console.log(palabra[i]);
    }
}

console.log(reverseString('Jonatan'));